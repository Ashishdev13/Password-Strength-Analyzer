# Password-Strength-Analyzer Dependencies
# Install with: pip install -r requirements.txt

# Colored terminal output (cross-platform)
colorama>=0.4.6

# All other modules used (hashlib, re, math, os, argparse, getpass) are Python standard library
